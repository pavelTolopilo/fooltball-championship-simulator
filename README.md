# Football Championship Simulator

This project built using React, Nest.js and WebSockets.

## Available Scripts

In the project directory, you can run:

### `make start-dev`

Runs the app in the development mode.\
Open [http://localhost:3000](http://localhost:3000) to view it in the browser.

The page will reload if you make edits.\
You will also see any lint errors in the console.

### `make start-test`

Launches the test runner in the interactive watch mode.

## Future update

- Transfer processing logic to the backend side.

- Add MongoDB with separate collections for:
  - teams;
  - matches;
  - scores.

- It's possible to additionally storing scores into redis for less delay.

- Add tests.